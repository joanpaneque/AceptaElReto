// Joan Paneque

#include <iostream>
using namespace std;

int main() {
    cin.sync_with_stdio(0);
    cin.tie(0);
    
    int n;
    while(1) {
        cin >> n;
        if (n == 0) break;

        
        for (int i = 0; i < n; i++) {
            cout << 1;
        }
        cout << "\n";
    }
    
}